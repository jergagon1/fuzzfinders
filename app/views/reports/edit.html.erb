


  <form class="set-location-form" action="/users/<%= current_user.id %>/reports/<%= @report.id %>/">

    <!--   <input type="hidden" name="_method" value="put"> -->
    <div class="container">
      <div id="map-canvas" style="width: 300px; height: 300px" data-report-type="<%= @report.report_type %>" data-pet-name="<%= @report.pet_name %>">
      </div>

    <input class="set-loc-btn btn btn-primary btn-orange" type="submit" value="Set Location">
    </div>
  </form>

<div class="container">
  Once you're set your location, click below:
  <form class="report-show" action="/users/<%= current_user.id %>/reports/<%= @report.id %>">
    <input class="view-rpt-btn btn btn-primary btn-orange" type="submit" value="View Report">
  </form>
</div>